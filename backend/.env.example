# FINX Environment Configuration Template
# Copy to .env or set in runtime environment

# Application Server
PORT=8080
SPRING_PROFILES_ACTIVE=dev

# PostgreSQL Database (Local or Supabase)
POSTGRES_DB=postgres
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_postgres_password 
POSTGRES_PORT=5432

# Direct Datasource Connection (Set for Supabase with sslmode=require)
SPRING_DATASOURCE_URL=jdbc:postgresql://your-project-id.pooler.supabase.com:5432/postgres?sslmode=require
SPRING_DATASOURCE_USERNAME=postgres.your_project_ref
SPRING_DATASOURCE_PASSWORD=your_supabase_password

# Supabase Disaggregated Parameters (Alternative)
SUPABASE_DB_HOST=your-project-id.pooler.supabase.com
SUPABASE_DB_PORT=5432
SUPABASE_DB_NAME=postgres
SUPABASE_DB_USERNAME=postgres.your_project_ref
SUPABASE_DB_PASSWORD=your_supabase_password

# Redis Configuration
REDIS_PORT=6379
SPRING_DATA_REDIS_HOST=localhost
SPRING_DATA_REDIS_PORT=6379
SPRING_DATA_REDIS_PASSWORD=

# Security & JWT Tokens
JWT_SECRET=your_256bit_or_greater_secret_key_here
JWT_ACCESS_TOKEN_EXPIRATION_MS=900000
JWT_REFRESH_TOKEN_EXPIRATION_MS=604800000

# CORS Allowed Origins (Comma-separated)
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Rate Limiting
RATE_LIMIT_ENABLED=true
RATE_LIMIT_REQUESTS_PER_MINUTE=60

# Google OAuth 2.0 Configuration
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret
# For local development: http://localhost:8080/api/auth/google/callback
# For Render production: https://finx-backend.onrender.com/api/auth/google/callback
GOOGLE_REDIRECT_URI=http://localhost:8080/api/auth/google/callback
# For local development: http://localhost:3000/auth/callback/google
# For Render production: https://finx-frontend.onrender.com/auth/callback/google
FRONTEND_OAUTH_CALLBACK_URL=http://localhost:3000/auth/callback/google

# Razorpay Fiat Payment Gateway Configuration
RAZORPAY_KEY_ID=your_razorpay_key_id
RAZORPAY_KEY_SECRET=your_razorpay_key_secret
RAZORPAY_WEBHOOK_SECRET=your_razorpay_webhook_secret
RAZORPAY_CURRENCY=INR
RAZORPAY_SANDBOX_MODE=true
